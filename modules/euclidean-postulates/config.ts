import { ModuleConfig } from '@/lib/types';

export const config: ModuleConfig = {
  slug: 'euclidean-postulates',
  title: 'Geometry Rules',
  description: 'Discover the 5 basic rules that all shapes and lines follow',
  subject: 'mathematics',
  grades: [3, 4, 5],
};
