import { ModuleConfig } from '@/lib/types';

export const config: ModuleConfig = {
  slug: 'shape-sorter',
  title: 'Shape Sorter',
  description: 'Drag shapes into matching outlines to learn geometry basics',
  subject: 'mathematics',
  grades: [0, 1],
};
