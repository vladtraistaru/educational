import { ModuleConfig } from '@/lib/types';

export const config: ModuleConfig = {
  slug: 'word-builder',
  title: 'Word Builder',
  description: 'Build words by dragging letters together and practice spelling',
  subject: 'literacy',
  grades: [0, 1],
};
