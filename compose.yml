services:
  educational:
    image: ghcr.io/vladtraistaru/educational:latest
    container_name: educational
    restart: unless-stopped
    env_file: .env          # lives on the server at /opt/apps/educational/.env
    networks: [proxy]
networks:
  proxy:
    external: true
