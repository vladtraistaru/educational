services:
  educational:
    image: ghcr.io/vladtraistaru/educational:latest
    container_name: educational
    restart: unless-stopped
    env_file: .env
    networks: [proxy]
    labels: ["com.centurylinklabs.watchtower.enable=true"]
networks:
  proxy:
    external: true
